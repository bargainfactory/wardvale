import type { Metadata, Viewport } from "next";
import { headers } from "next/headers";
import { ThemeProvider } from "@/components/theme-provider";
import { LocaleProvider } from "@/lib/locale-context";
import { StartExperienceProvider } from "@/components/start-experience/provider";
import { AnalyticsProvider } from "@/components/analytics-provider";
import { PwaRegister } from "@/components/pwa-register";
import { locales, type Locale } from "@/lib/i18n";
import "./globals.css";

export const viewport: Viewport = {
  themeColor: "#050a14",
};

const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "https://wardvale.com";

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  title: {
    default:
      "Wardvale — The AI Back Office for Small Business | You Approve Every Send",
    template: "%s | Wardvale",
  },
  description:
    "Wardvale's AI agents draft the follow-ups your day never leaves room for — quote chases, review replies, win-backs, payment reminders — and nothing sends until you tap approve. From $500/mo.",
  keywords: [
    "AI back office",
    "AI agents for small business",
    "quote follow-up automation",
    "customer win-back automation",
    "review response AI",
    "no-show reduction",
    "invoice follow-up automation",
    "AI automation for restaurants",
    "AI agents for salons",
    "AI for contractors",
    "human-in-the-loop AI",
    "Wardvale",
    "done for you automation",
    "small business AI",
    "AI approval workflow",
    "abandoned cart recovery",
  ],
  authors: [{ name: "Wardvale" }],
  creator: "Wardvale",
  publisher: "Wardvale",
  robots: {
    index: true,
    follow: true,
    "max-image-preview": "large",
    "max-snippet": -1,
    "max-video-preview": -1,
    googleBot: {
      index: true,
      follow: true,
    },
  },
  openGraph: {
    type: "website",
    locale: "en_US",
    url: siteUrl,
    siteName: "Wardvale",
    title: "Wardvale — The AI Back Office for Small Business",
    description:
      "AI agents draft the follow-ups you never get to send — quote chases, win-backs, review replies. You approve every message. From $500/mo.",
    // Social card image is generated by app/opengraph-image.tsx (file convention).
  },
  twitter: {
    card: "summary_large_image",
    title: "Wardvale — The AI Back Office for Small Business",
    description:
      "AI agents draft the follow-ups you never get to send. You approve every message. From $500/mo.",
    creator: "@wardvaleai",
  },
  // No global canonical — it would force every page to canonicalize to "/".
  // Pages without an explicit canonical self-canonicalize to their own URL
  // (Google's default), which is correct alongside the per-page hreflang.
  verification: {
    google: process.env.GOOGLE_SITE_VERIFICATION ?? undefined,
  },
};

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": `${siteUrl}/#org`,
      name: "Wardvale",
      url: siteUrl,
      logo: `${siteUrl}/logo.svg`,
      description:
        "The AI back office for small businesses: agents that draft quote follow-ups, win-backs, review replies and payment reminders — every message approved by the owner before it sends.",
      contactPoint: {
        "@type": "ContactPoint",
        email: "hello@wardvale.com",
        contactType: "sales",
        availableLanguage: ["English", "Spanish", "French", "Portuguese", "German"],
      },
      sameAs: [
        "https://twitter.com/wardvaleai",
        "https://linkedin.com/company/wardvaleai",
      ],
    },
    {
      "@type": "WebSite",
      "@id": `${siteUrl}/#website`,
      url: siteUrl,
      name: "Wardvale",
      publisher: { "@id": `${siteUrl}/#org` },
      inLanguage: ["en", "es", "fr", "pt", "de"],
    },
    {
      "@type": "Service",
      "@id": `${siteUrl}/#service`,
      name: "AI Automation Retainer",
      provider: { "@id": `${siteUrl}/#org` },
      serviceType: "AI Automation Agency",
      description:
        "Monthly automation retainers including Zapier flows, custom GPT agents, inbox triage, lead capture, onboarding, and more for small businesses.",
      offers: [
        {
          "@type": "Offer",
          name: "Starter",
          price: "500",
          priceCurrency: "USD",
          description:
            "1 flagship automation + inbox triage agent. Ideal for solo operators.",
        },
        {
          "@type": "Offer",
          name: "Growth",
          price: "2000",
          priceCurrency: "USD",
          description:
            "Up to 5 automations + custom GPT agent. Full-funnel coverage.",
        },
        {
          "@type": "Offer",
          name: "Scale",
          price: "5000",
          priceCurrency: "USD",
          description:
            "Unlimited automations, dedicated agent engineer, priority everything.",
        },
      ],
      areaServed: { "@type": "Place", name: "Worldwide" },
      audience: {
        "@type": "Audience",
        audienceType:
          "Small business owners: restaurants, e-commerce stores, consultants, local service businesses",
      },
    },
    // NOTE: FAQPage structured data is emitted ONLY on /pricing (where the FAQ is
    // actually visible), in app/pricing/layout.tsx — Google requires the marked-up
    // Q&A to be present on the page. It must not live in this global @graph.
  ],
};

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const h = await headers();
  const locale = ((h.get("x-locale") as Locale | null) ?? "en") as Locale;
  const path = h.get("x-pathname") ?? "/";
  const hreflangs = locales.map((l) => ({
    code: l,
    href: `${siteUrl}${l === "en" ? "" : `/${l}`}${path === "/" ? "" : path}`,
  }));

  return (
    <html lang={locale} suppressHydrationWarning>
      <head>
        {hreflangs.map((a) => (
          <link key={a.code} rel="alternate" hrefLang={a.code} href={a.href} />
        ))}
        <link rel="alternate" hrefLang="x-default" href={`${siteUrl}${path === "/" ? "" : path}`} />
        <link rel="preconnect" href="https://rsms.me/" />
        <link rel="stylesheet" href="https://rsms.me/inter/inter.css" />
        <link
          rel="stylesheet"
          href="https://api.fontshare.com/v2/css?f[]=satoshi@400,500,600,700&display=swap"
        />
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
        />
      </head>
      <body>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          <LocaleProvider initialLocale={locale}>
            <StartExperienceProvider>{children}</StartExperienceProvider>
            <AnalyticsProvider />
            <PwaRegister />
          </LocaleProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
